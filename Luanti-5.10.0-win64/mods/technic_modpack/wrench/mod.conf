name = wrench
depends = default
optional_depends = technic, technic_chests, technic_worldgen, intllib
description = Adds a wrench that allows the player to pickup nodes that contain an inventory with items or metadata that needs perserving.